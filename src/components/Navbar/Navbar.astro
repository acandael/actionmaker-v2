---
import { Image } from 'astro:assets';
import { Button } from '@/components/ui/button';
import { ArrowRight } from 'lucide-react';
import MobileMenu from './MobileMenu.astro';
import { LanguageSelector } from 'astro-i18next/components';
import { t, setDefaultNamespace } from 'i18next';
import { localizePath } from 'astro-i18next';

// Import your logo image
import logo from '/src/assets/logo.png';

// Initialize i18next with navbar namespace
setDefaultNamespace('navbar');

const links = [
  { href: '/', label: t('home') },
  { href: '/teambuildings', label: t('teambuildings') },
  { href: '/concepten', label: t('concepten') },
  { href: '/over-actionmaker', label: t('over-actionmaker') },
  { href: '/contact', label: t('contact') },
];
---

export interface Props {
  // Add any props if needed
}

<header
  class="fixed top-0 left-0 right-0 h-20 bg-black backdrop-blur-lg z-50 border-b overflow-hidden border-white/10"
>
  <div class="max-w-7xl mx-auto h-full px-4 flex items-center justify-between">
    <a href={localizePath('/')} class="flex items-center gap-2">
      <Image src={logo} alt="ActionMaker" width={150} height={84} />
    </a>

    <nav class="hidden md:flex items-center gap-8">
      {
        links.map(({ href, label }) => (
          <a
            href={localizePath(href)}
            class="relative text-sm font-medium text-white/90 hover:text-white transition-colors group inline-block"
          >
            {label}
            <span
              class="absolute -bottom-1 left-0 w-0 h-0.5 bg-red-500 transition-all duration-300 group-hover:w-full"
              aria-hidden="true"
            />
          </a>
        ))
      }
    </nav>

    <LanguageSelector
      showFlag={true}
      class="hidden md:block px-3 py-2 text-sm text-white/90 hover:text-white border border-white/10 rounded-md bg-white/5 hover:bg-white/10 transition-colors duration-300"
    />

    <div class="flex items-center gap-4">
      <div class="hidden md:block">
        <Button asChild>
          <a href={localizePath('/contact')} class="flex items-center gap-2">
            {t('navbar:find-teambuilding')}
            <ArrowRight className="w-4 h-4" />
          </a>
        </Button>
      </div>

      <button
        type="button"
        class="md:hidden p-2 -mr-2 text-white/70 hover:text-white transition-colors"
        data-mobile-menu-trigger
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="currentColor"
          class="w-6 h-6"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"></path>
        </svg>
      </button>
    </div>
  </div>
</header>
<MobileMenu />

<script>
  function initializeMobileMenu() {
    const mobileMenuTrigger = document.querySelector('[data-mobile-menu-trigger]');
    const mobileMenu = document.getElementById('mobile-menu');
    let isMenuOpen = false;

    // Remove existing event listener if any
    const newTrigger = mobileMenuTrigger?.cloneNode(true);
    if (mobileMenuTrigger?.parentNode && newTrigger) {
      mobileMenuTrigger.parentNode.replaceChild(newTrigger, mobileMenuTrigger);
    }

    newTrigger?.addEventListener('click', () => {
      isMenuOpen = !isMenuOpen;

      if (isMenuOpen) {
        mobileMenu?.classList.remove('translate-x-full');
        document.body.style.overflow = 'hidden';
      } else {
        mobileMenu?.classList.add('translate-x-full');
        document.body.style.overflow = '';
      }
    });
  }

  // Initialize on page load
  initializeMobileMenu();

  // Re-initialize on view transitions
  document.addEventListener('astro:page-load', initializeMobileMenu);
</script>
