---
import { Button } from "@/components/ui/button";
import { ArrowRight } from "lucide-react";
---

<section class="relative py-32 px-4 overflow-hidden bg-gradient-to-b from-background to-muted/50">
  {/* Background Pattern */}
  <div class="absolute inset-0 opacity-40">
    <div class="absolute inset-0 bg-grid-white/10"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-background via-transparent to-transparent"></div>
  </div>

  <div class="relative max-w-3xl mx-auto text-center">
    <h2 class="text-3xl md:text-4xl font-bold mb-6">
      Klaar om je team te versterken?
    </h2>
    
    <p class="text-lg text-muted-foreground mb-12 max-w-2xl mx-auto">
      Neem vandaag nog contact op met Action Maker. Binnen twee werkdagen ontvang je een
      offerte op maat voor jouw team.
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <Button 
        size="lg"
        className="bg-red-600 hover:bg-red-700 text-white group px-8 py-6 text-lg rounded-xl font-medium shadow-lg hover:shadow-xl transition-all duration-300"
      >
        Plan je teambuilding
        <ArrowRight className="ml-2 h-5 w-5 transition-transform group-hover:translate-x-1" />
      </Button>
      <Button 
        size="lg"
        variant="outline"
        className="border-2 px-8 py-6 text-lg rounded-xl font-medium hover:bg-muted/50 transition-colors duration-300"
      >
        Bekijk activiteiten
      </Button>
    </div>

    {/* Contact Info */}
    <div class="mt-12 text-muted-foreground">
      <p class="text-sm">
        Of bel ons direct op{' '}
        <a 
          href="tel:+32485462505" 
          class="text-red-600 hover:text-red-700 transition-colors"
        >
          0485 46 25 05
        </a>
      </p>
    </div>
  </div>
</section>